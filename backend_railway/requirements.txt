# DezAI Backend Requirements for Railway
# Optimized for cloud deployment with Demucs 8 stems

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Audio processing - CPU versions for Railway compatibility
torch==2.1.0+cpu
torchaudio==2.1.0+cpu
demucs==4.0.1
librosa==0.10.1
soundfile==0.12.1
numpy==1.24.3

# File handling and utilities
pydantic==2.5.0
python-dotenv==1.0.0
aiohttp==3.9.1

# System dependencies (will be installed by Railway/Nixpacks)
# ffmpeg, libsndfile1

# Note: Using CPU versions of PyTorch for Railway compatibility
# Railway doesn't have GPU by default, but you can upgrade later
