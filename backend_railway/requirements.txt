# DezAI Backend Requirements for Railway
# Optimized for cloud deployment with Demucs 8 stems

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Audio processing
demucs==4.0.1
torch==2.1.0
torchaudio==2.1.0
librosa==0.10.1
soundfile==0.12.1
numpy==1.24.3

# File handling and utilities
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
aiohttp==3.9.1

# Optional: for better performance
pillow==10.1.0
scipy==1.11.4
resampy==0.4.2

# Development (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Note: Railway will automatically install CUDA-enabled PyTorch if GPU is available
# For CPU-only deployment, use these instead:
# torch==2.1.0+cpu
# torchaudio==2.1.0+cpu
# --extra-index-url https://download.pytorch.org/whl/cpu